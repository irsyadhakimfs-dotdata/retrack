# Dependensi PENGEMBANGAN LOKAL (superset dari requirements.txt).
# Pakai ini di mesin dev: pip install -r requirements-dev.txt
-r requirements.txt

# Sumber data market utama (opsional; di Vercel tidak dipasang).
yfinance==0.2.55

# Testing
pytest==8.3.5
